Regression note: Preview test-asset deposits must be able to recover the real token color from `blackout-safe:test-asset-manifest:v1` and the fixed Preview test deposit amount (100000 base units) when the rendered form has been reset. This is not synthetic state: the color must come from the real minted test-asset manifest and the deposit transaction must still use `depositShielded`.
